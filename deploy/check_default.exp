#!/usr/bin/expect -f

set timeout 60

spawn ssh ubuntu@118.25.141.173
expect "password:"
send "Chris@1989\r"

expect -re {\$ |# }
send "sudo nginx -T 2>&1 | grep -B5 'default_server' | head -20\r"

expect -re {\$ |# }
send "sudo cat /etc/nginx/sites-available/bajie\r"

expect -re {\$ |# }
send "dig rixin.velolabs.top +short\r"

expect -re {\$ |# }
send "exit\r"

expect eof