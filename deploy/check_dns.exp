#!/usr/bin/expect -f

set timeout 60

spawn ssh ubuntu@118.25.141.173
expect "password:"
send "Chris@1989\r"

expect -re {\$ |# }
send "sudo ls -la /etc/nginx/sites-enabled/\r"

expect -re {\$ |# }
send "sudo grep -r 'rixin.velolabs' /etc/nginx/sites-enabled/ 2>&1\r"

expect -re {\$ |# }
send "sudo grep -r 'velolabs' /etc/nginx/sites-enabled/ | grep server_name | head -20\r"

expect -re {\$ |# }
send "sudo netstat -tlnp | grep LISTEN\r"

expect -re {\$ |# }
send "exit\r"

expect eof