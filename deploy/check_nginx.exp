#!/usr/bin/expect -f

set timeout 60

spawn ssh ubuntu@118.25.141.173
expect "password:"
send "Chris@1989\r"

expect -re {\$ |# }
send "sudo cat /etc/nginx/sites-enabled/rixin\r"

expect -re {\$ |# }
send "sudo nginx -T 2>&1 | grep -A20 'server_name rixin'\r"

expect -re {\$ |# }
send "curl -H 'Host: rixin.velolabs.top' http://localhost/ 2>&1 | head -10\r"

expect -re {\$ |# }
send "exit\r"

expect eof