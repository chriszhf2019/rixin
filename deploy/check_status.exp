#!/usr/bin/expect -f

set timeout 120

spawn ssh ubuntu@118.25.141.173
expect "password:"
send "Chris@1989\r"

expect -re {\$ |# }
send "sleep 10 && curl -I http://localhost:3000 2>&1\r"

expect -re {\$ |# }
send "sudo pm2 status\r"

expect -re {\$ |# }
send "sudo pm2 logs rixin --lines 20\r"

expect -re {\$ |# }
send "exit\r"

expect eof