#!/usr/bin/expect -f

set timeout 60

spawn ssh ubuntu@118.25.141.173
expect "password:"
send "Chris@1989\r"

expect -re {\$ |# }
send "sudo lsof -i :3000\r"

expect -re {\$ |# }
send "sudo netstat -tlnp | grep 3000\r"

expect -re {\$ |# }
send "ps aux | grep 'node.*server.js' | head -10\r"

expect -re {\$ |# }
send "sudo pm2 list\r"

expect -re {\$ |# }
send "curl -v http://localhost:3000/today 2>&1 | head -20\r"

expect -re {\$ |# }
send "exit\r"

expect eof