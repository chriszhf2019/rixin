#!/usr/bin/expect -f

set timeout 600

# 先上传脚本
spawn scp /Volumes/z/202606/Rixin/deploy/fix_and_build.sh ubuntu@118.25.141.173:/tmp/
expect "password:"
send "Chris@1989\r"
expect eof

# SSH 执行
spawn ssh ubuntu@118.25.141.173
expect "password:"
send "Chris@1989\r"

expect -re {\$ |# }
send "chmod +x /tmp/fix_and_build.sh && bash /tmp/fix_and_build.sh 2>&1 | tail -60\r"

expect -re {\$ |# }
send "exit\r"

expect eof