#!/usr/bin/expect -f

set timeout 600

spawn scp /Volumes/z/202606/Rixin/deploy/full_fix.sh ubuntu@118.25.141.173:/tmp/
expect "password:"
send "Chris@1989\r"
expect eof

spawn ssh ubuntu@118.25.141.173
expect "password:"
send "Chris@1989\r"

expect -re {\$ |# }
send "sudo bash /tmp/full_fix.sh 2>&1\r"

expect {
    "DONE" {
        puts "\n=== FULL FIX DEPLOY COMPLETE ==="
    }
    timeout {
        puts "\n=== TIMEOUT ==="
    }
}

expect -re {\$ |# }
send "exit\r"

expect eof